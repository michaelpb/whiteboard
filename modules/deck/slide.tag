<slide>
    <div each={opts.panes} class="outer-pane">
        <!-- Generate mount points for each pane contents -->
        <div id="{mount_id}"></div>
    </div>

    <!--
        <script>
            this.on('updated', () => {
            });
            this.on('mount', () => {
            });
        </script>
    -->
</slide>
